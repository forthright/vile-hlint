machine:
  node:
    version: 5.11.1

dependencies:
  pre:
    - sh bin/check-libstdc-deb
    - sh bin/list-env-versions
  post:
    - npm run compile

test:
  override:
    - echo "TODO"
  post:
    - time npm run vile
